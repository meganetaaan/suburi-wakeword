{
  "name": "suburi-wakeword",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wake word training and voice collection pipeline for Stack-chan experiments.",
  "scripts": {
    "docs:check": "node scripts/check-docs.mjs",
    "pipeline:test": "cd training/pipeline && PYTHONPATH=src uv run python -m unittest discover -s tests -v",
    "pipeline:smoke": "cd training/pipeline && PYTHONPATH=src uv run python -m suburi_wakeword.run_smoke_pipeline",
    "ci": "pnpm docs:check && pnpm pipeline:test"
  },
  "devDependencies": {}
}
