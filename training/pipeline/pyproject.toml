[project]
name = "suburi-wakeword-pipeline"
version = "0.1.0"
description = "Smoke TTS/data/training pipeline for suburi wake word experiments"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
]

[tool.uv]
package = false
